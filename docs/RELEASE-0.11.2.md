# Olympus 0.11.2

Command now keeps its 3D renderer across mode changes, without falling back to the legacy Omega/orbits. Equivalent data refreshes retain meshes; graphics recovery offers a 3D retry. Live scale is 0.896 while the material study stays at 1.4.

The ring has three clean structural systems, refined chassis depth, narrower recessed module joints, and fewer decorative marks. Four selective amber energy segments and softly illuminated project names restore emphasis. Thinking orbitals now run at 14 times idle speed. Minimum 1.5x render resolution improves edges on standard-density screens.

Validation: production TypeScript/Vite build passed; all 28 browser renderer harness checks passed, including context recovery, missing WebGL, project hit geometry, constellation preservation, reduced motion and hidden rendering. Visual review used the material study and browser live shell. Real desktop vault/voice interactions were not automated in this release pass. No persistence schema or model changes.
